// Declare boolean indicating if Timmy has a developer job
const hasDeveloperJob = true;

// Check if Timmy is employed as a developer
if (hasDeveloperJob) {
    console.log("Timmy is employed as a developer."); // Runs if hasDeveloperJob is true
}

// Declare boolean indicating if Timmy is a gamer
const isTimmyAGamer = false;

// Check if Timmy is a gamer
if (isTimmyAGamer) {
    console.log("Timmy loves to play World of Warcraft."); // Runs if isTimmyAGamer is true
}

// Declare Timmy's age
const timmyAge = 13;

// Check if Timmy is old enough to drive (minimum age: 16)
if (timmyAge >= 16) {
    console.log("Timmy is old enough to drive."); // Runs if age is 16 or older
} else {
    console.log("Timmy is not old enough to drive."); // Runs if age is below 16
}